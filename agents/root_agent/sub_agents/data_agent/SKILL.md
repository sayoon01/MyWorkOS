# 절차: 데이터 분석 요청 처리
1. 파일 경로 미지정 시 load_csv 호출하지 않고 먼저 파일 확인
2. 경로 확보되면 load_csv 호출 → 컬럼/행수/결측치 현황 확인
3. 결측치·이상치 발견 시 임의 처리 없이 먼저 보고
4. 통계 요청이면 summarize_stats 호출, 대상 컬럼 불명확하면 되묻기
5. "차트/그래프" 요청에만 export_chart 호출
